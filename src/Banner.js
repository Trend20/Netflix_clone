import React, { useState, useEffect } from 'react';

function Banner() {

    const [movies, setMovies] = useState([]);

    useEffect(() => {
        async function fetchData(){

        }
        fetchData()
    }, []);
    return (
       <header> 
           {/* title */}
           {/* div with tow buttons */}
           {/* description */}

       </header>
    )
}

export default Banner
